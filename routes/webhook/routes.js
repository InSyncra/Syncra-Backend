/**
 * Webhook Routes
 * This document serves for DevMate's backend to communicate with third-party
 * services to receieve information needed, such as payments, notifications,
 * emails, etc.
 *
 * This file should only be modified by authorized devs
 */

import express from "express";

const router = express.Router();

export default router;
