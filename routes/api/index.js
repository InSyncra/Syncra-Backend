/**
 * API Routes
 * Frontend will use the endpoint api/v1/{endpoint} in order to access
 * and retrieve required information
 *
 */

const express = require("express");
const api = express.Router();

// TODO: implement user auth middleware, add it here

module.exports = api;
